version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run: echo "Hello CircleCI!"

workflows:
  version: 2
  build_workflow:
    jobs:
      - build
